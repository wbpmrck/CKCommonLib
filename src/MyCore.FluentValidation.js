/*
作者： kaicui
依赖： MyCore.Validate
使用说明：用于进行可以链式调用的验证效果
侵入性：根据验证扩展不同，分别侵入string、number等类的对象

版本历史：

2012年3月29日20:08:47      版本创建
//////////////////////////////////////////////////////////////////*/

var MyCore = (function (my) {
    return my;
} (MyCore || {}))

MyCore.FluentValidation = (function (my) {

    return my;
} (MyCore.FluentValidation || {}));